> **This file is superseded.** Run `dibs init` in your repo to install
> the coordinator integration block into `./AGENTS.md`.
>
> The block is managed: it is wrapped in `<!-- BEGIN/END AF-COORDINATOR
> INTEGRATION v:1 -->` markers. Text outside the markers is never touched.
> Re-running `dibs init` replaces stale blocks in place, so the block
> stays current after an `dibs` upgrade.
>
> For the full agent protocol: `dibs protocol`.
