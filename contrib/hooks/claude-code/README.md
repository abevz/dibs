# Claude Code integration

See [one-shot agent integration](../README.md). Run `dibs hooks install --agent claude` in the repository. The installed project hook is `.claude/settings.json` → `hooks.SessionStart`; it displays ready work without claiming it.
